<% page = primary_body %>
<%= image_tag page.image_elements.named('Small Content Image').public_filename(:medium_thumb) unless page.image_elements.empty? or !page.image_elements.named('Small Content Image').filename%>
<%=t page.text_elements.named('Body Text').content %>
