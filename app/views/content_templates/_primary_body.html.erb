<% page = primary_body %>
<%=r page.text_elements.named('Body Text').content %>
<%= image_tag( page.image_elements.named('Small Content Image').public_filename(:medium_thumb), :class => 'float-right' ) unless page.image_elements.empty? or !page.image_elements.named('Small Content Image').filename%>
